describe("GlobalExceptionFilter", () => {
  it.todo("should return 404 page for non existing url (GET)");
  it.todo("should return 404 json for non existing url (non GET)");

  it.todo("should return 401 json for 401 POST");

  it.todo("should return 404 page if for GET 401 ");

  it.todo("should return 500 page for server error (GET)");
  it.todo("should return 500 json for server error (non GET)");
});
